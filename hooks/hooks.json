{
  "description": "Goal Mode plugin hooks. Stop fires after every assistant turn to advance the plan-tree state. SessionStart fires on every new CC session to auto-inject the current continuation prompt when an active pursuing goal exists.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/stop-hook.sh\""
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start-hook.sh\""
          }
        ]
      }
    ]
  }
}

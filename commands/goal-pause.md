---
description: "Pause the active goal (Stop hook will exit cleanly while paused)"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/pause-goal.sh:*)"]
---

# Goal Pause

```!
"${CLAUDE_PLUGIN_ROOT}/scripts/pause-goal.sh"
```

---
description: "Resume the paused goal (refuses if budget is exhausted)"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/resume-goal.sh:*)"]
---

# Goal Resume

```!
"${CLAUDE_PLUGIN_ROOT}/scripts/resume-goal.sh"
```

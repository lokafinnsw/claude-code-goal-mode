---
description: "Abandon the active goal (lifecycle → unmet with reason)"
argument-hint: "--reason \"...\""
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/abandon-goal.sh:*)"]
---

# Goal Abandon

```!
"${CLAUDE_PLUGIN_ROOT}/scripts/abandon-goal.sh" $ARGUMENTS
```

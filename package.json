{
  "name": "claude-code-goal-mode",
  "version": "1.1.3",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint:sh": "bash -c 'shopt -s nullglob; files=(hooks/*.sh scripts/*.sh); [[ ${#files[@]} -gt 0 ]] && shellcheck \"${files[@]}\" || echo \"no .sh files to check\"'"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}

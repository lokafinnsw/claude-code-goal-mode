{
  "description": "Goal Mode plugin Stop hook — invokes the Node engine to drive long-horizon plan-tree execution. Reads JSON stdin from Claude Code, returns JSON stdout for the continuation loop.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/stop-hook.sh\""
          }
        ]
      }
    ]
  }
}

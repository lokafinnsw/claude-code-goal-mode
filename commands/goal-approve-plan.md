---
description: "Validate the draft plan and mark it approved (lifecycle: draft → approved)"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/approve-plan.sh:*)"]
---

# Goal Approve Plan

```!
"${CLAUDE_PLUGIN_ROOT}/scripts/approve-plan.sh"
```
